#include "main.h"

/**
 * print_most_numbers - numbers, from 0 to 9, followed by a new line
 *
 * Return: Always 0
 */

void print_most_numbers(void)
{
int n;

for (n = 48; n < 58)
{
	if (n != 2 && n != 4; n++)

	{
	_putchar(n);
	}
}
_putchar('\n');
}
